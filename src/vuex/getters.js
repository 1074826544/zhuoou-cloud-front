import common from '@/utils/common';
const getters = {
     //getCount: state => state.count,
     //menuList: state => common.constructTree(OperatorUtils.getMenuData(), 'title')
}

export default getters
